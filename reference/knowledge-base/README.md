# Knowledge base (living)

Refresh: `bash scripts/kb_refresh.sh`  
Sources: [`sources.json`](sources.json)  
Skill: `context7-cli-stable` (+ Firecrawl portals listed in sources).

Topics land under `topics/`; raw scrapes under `raw/`. No secrets, no site PII.
