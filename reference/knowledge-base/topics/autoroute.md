<!-- seed; refresh via scripts/kb_refresh.sh -->

# Autoroute (design digest)

Hybrid: telemetry → ADK agent (clamps + NS/seismo priors) → `meta/patches/<nodeId>.json`.

Schema + clamps: `docs/autoroute.md`. Physics: `docs/physics.md`. Colab: `docs/colab-gemini-pipeline.md`.
