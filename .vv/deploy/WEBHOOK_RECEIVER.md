# Webhook receiver (issue #37 follow-up)

Added runnable `services/vercel-webhook-receiver/` (HMAC → repository_dispatch).
Actions workflow `vercel-webhook.yml` summarizes + comments on #37.
Secret **names** only — no values recorded.
