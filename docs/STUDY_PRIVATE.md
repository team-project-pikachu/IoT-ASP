# Private study package (pointer only)

Site protocol, ethics TBD, Gemini Enterprise 5-seat roster, and recording upload ops live in the **local** `study/` directory of this repository.

`study/` is **gitignored** — never commit street addresses, neighbor identifiers, or recording URIs to the public default branch.

Public scientific tooling (blaster PWA, ADK autoroute agent, Colab stub, literature) stays on `main`.
